# Copy this file to .env.local (or .env) and update the backend URL below.
# The value must include the protocol (http or https) and should NOT end with a trailing slash.
REACT_APP_BACKEND_URL=https://your-ngrok-subdomain.ngrok-free.dev
