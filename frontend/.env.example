# Copy this file to .env.local (or .env) and update the API base URL below.
# The value must include the protocol (http or https) and should include the `/api` suffix.
REACT_APP_API_URL=https://your-ngrok-subdomain.ngrok-free.dev/api
